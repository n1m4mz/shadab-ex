# راهنمای راه‌اندازی اتصال به تلگرام

این راهنما به شما کمک می‌کند تا سایت را به کانال تلگرام خود متصل کنید.

## روش 1: استفاده از Telegram Bot API (پیشنهادی)

### مرحله 1: ساخت ربات تلگرام

1. به [@BotFather](https://t.me/botfather) در تلگرام بروید
2. دستور `/newbot` را ارسال کنید
3. نام ربات را وارد کنید (مثلاً: `Shadab Exchange Bot`)
4. نام کاربری ربات را وارد کنید (مثلاً: `shadab_exchange_bot`)
5. توکن ربات را کپی کنید (مثلاً: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### مرحله 2: اضافه کردن ربات به کانال

1. کانال تلگرام خود را باز کنید
2. روی نام کانال کلیک کنید
3. به بخش "Administrators" بروید
4. روی "Add Administrator" کلیک کنید
5. نام کاربری ربات را جستجو و اضافه کنید
6. دسترسی "Post Messages" را به ربات بدهید

### مرحله 3: دریافت Channel ID

برای دریافت Channel ID، یکی از روش‌های زیر را استفاده کنید:

#### روش A: استفاده از ربات @userinfobot

1. ربات [@userinfobot](https://t.me/userinfobot) را به کانال اضافه کنید
2. یک پیام در کانال ارسال کنید
3. ربات Channel ID را به شما می‌دهد

#### روش B: استفاده از API تلگرام

1. یک پیام در کانال ارسال کنید
2. از این URL استفاده کنید (توکن ربات را جایگزین کنید):
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
3. در پاسخ JSON، `chat.id` را پیدا کنید

### مرحله 4: تنظیم متغیرهای محیطی

فایل `.env.local` را در ریشه پروژه ایجاد کنید:

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHANNEL_ID=@your_channel_username
```

یا اگر Channel ID عددی است:

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHANNEL_ID=-1001234567890
```

## روش 2: استفاده از RSS Feed

اگر کانال شما RSS Feed دارد:

1. URL RSS Feed را پیدا کنید (مثلاً از [rss.app](https://rss.app))
2. در `.env.local` اضافه کنید:

```env
TELEGRAM_RSS_URL=https://rss.app/feeds/your_channel_id.xml
```

## روش 3: استفاده از API سفارشی

اگر یک سرویس API سفارشی دارید:

```env
TELEGRAM_API_URL=https://api.example.com/telegram/channel
```

API شما باید یک JSON با این ساختار برگرداند:

```json
{
  "message": "متن پیام کانال",
  "text": "متن پیام کانال"
}
```

## فرمت پیام در کانال

پیام‌های شما باید به این فرمت دقیق باشند:

```
صرافی شاداب 🇩🇪
📅 تاریخ: یکشنبه 12 دلو ۱۴۰۴
📆 مصادف: 01 February  2026

🔻 نرخ امروز:

1️⃣ 1 Euro = 75,50   🇦🇫 افغانی
2️⃣ 1 Euro = 1,1490  🇺🇸 دالر
3️⃣ 1 Euro = 179,000 🇮🇷 تومان
4️⃣ 1 Euro = 50,80 🇹🇷 لیر ترکیه

✅ خدمات سریع و مطمئن در تبدیل ارز و ارسال حواله
📲 واتساپ: ‎+93 79 911 0081

صرافی شاداب، پلی اعتماد میان شما و جهان 🌍
```

### نکات مهم:

- ✅ تاریخ شمسی و میلادی باید دقیقاً به این فرمت باشند
- ✅ هر نرخ باید در یک خط جداگانه باشد
- ✅ فرمت: `شماره + 1 Euro = نرخ + فلگ + نام ارز`
- ✅ می‌توانید ارزهای بیشتری اضافه کنید

## تست اتصال

پس از تنظیم متغیرهای محیطی:

1. سرور توسعه را راه‌اندازی کنید: `npm run dev`
2. به آدرس `http://localhost:3000/api/exchange-rates` بروید
3. باید داده‌های پارس شده را ببینید

## عیب‌یابی

### مشکل: داده‌ها بارگذاری نمی‌شوند

- ✅ بررسی کنید که `.env.local` درست تنظیم شده باشد
- ✅ بررسی کنید که ربات به کانال دسترسی دارد
- ✅ بررسی کنید که Channel ID درست است
- ✅ لاگ‌های سرور را بررسی کنید

### مشکل: داده‌ها پارس نمی‌شوند

- ✅ فرمت پیام را بررسی کنید
- ✅ مطمئن شوید که پیام به فرمت دقیق بالا است
- ✅ بررسی کنید که فلگ‌های ارز درست هستند

## پشتیبانی

اگر مشکلی داشتید، لاگ‌های کنسول را بررسی کنید یا با تیم توسعه تماس بگیرید.
